<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <a class="logo">
      <img src="/img/logo-SextoSol.png" width='100' alt='Tu cine'>  
       </a>
  <body>
    <h1>EDITAR PELICULAS</h1>
    <form action="/editMovies/<%= movie.id %>?_method=PUT" method="POST">
      <div>
        <label for="title">Título</label>
        <input type="text" name="title" id="title" value="<%= movie.title %>" />
        <% if (locals.errors && errors.title) { %>
        <p class="text"><%= errors.title.msg %></p>
        <% } %>
      </div>
      <div>
        <label for="rating">Rating</label>
        <input
          type="number"
          name="rating"
          id="rating"
          value="<%= movie.rating %>"
        />
        <% if (locals.errors && errors.rating) { %>
        <p class="text"><%= errors.rating.msg %></p>
        <% } %>
      </div>
      <div>
        <label for="awards">Premios</label>
        <input
          type="number"
          name="awards"
          id="awards"
          value="<%= movie.awards %>"
        />
        <% if (locals.errors && errors.awards) { %>
        <p class="text"><%= errors.awards.msg %></p>
        <% } %>
      </div>
      <div>
        <label for="release_date">Fecha de estreno</label>
        <input
          type="date"
          name="release_date"
          id="release_date"
          value="<%= movie.release_date %>"
        />
        <% if (locals.errors && errors.release_date) { %>
        <p class="text"><%= errors.release_date.msg %></p>
        <% } %>
      </div>
      <div>
        <label for="length">Duración</label>
        <input
          type="number"
          name="length"
          id="length"
          value="<%= movie.length %>"
        />
        <% if (locals.errors && errors.length) { %>
        <p class="text"><%= errors.length.msg %></p>
        <% } %>
      </div>
      <div>
        <label for="">Género</label>
        <select name="genre" id="genre">
          <% for (let i=0; i < genre.length; i++) { %> <% if
          (genre[i].id==movie.genre_id) { %>
          <option value="<%= genre[i].id %>" selected>
            <%= genre[i].name %>
          </option>
          <% } else { %>
          <option value="<%= genre[i].id %>"><%= genre[i].name %></option>
          <% } %> <% } %>
        </select>
      </div>
      <% if (!locals.user) { %>
      <button><a href="/">Volver al listado de peliculas</a></button>

      <% } else { %>
      <button>
        <a href="/deleteMovies/<%= detail.id %>">Eliminar película</a>
      </button>
      <button>
        <a href="/editMovies/<%= detail.id %>">Modificar Película</a>
      </button>
      <button><a href="/">Volver al listado de películas</a></button>
      <% } %>
    </section>
     
  </body>
</html>
